{
  "name": "notifyengine",
  "private": true,
  "packageManager": "npm@11.3.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "lint:root": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
